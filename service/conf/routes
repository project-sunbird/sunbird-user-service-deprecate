# Routes
# This file defines all application routes (Higher priority routes first)
# OPTIONS    /*all                  @controllers.UserController.preflight(all)
# ~~~~
POST      /v1/user/create	   @controllers.UserController.create()
PATCH     /v1/user/update	   @controllers.UserController.update()